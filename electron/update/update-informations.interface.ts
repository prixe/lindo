export interface UpdateInformations {
    noemu: {
        version: string;
        required: boolean;
        web: boolean;
    };
    dofustouch: {
        version: string;
        file: string;
        fileName: string;
    }
}