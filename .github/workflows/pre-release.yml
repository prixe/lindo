name: CI/CD pre release
on:
  push:
    branches: [ master ]

jobs:

  release-win:
    name: "Release windows"
    runs-on: ubuntu-20.04

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      #utiliser node v14 ? https://github.com/actions/setup-node
      - run: npm i
      - run: npm run build:prod
      - run: docker run -v ${PWD}:/project electronuserland/builder:wine /bin/bash -c "npm run release:win"

      - name: Extract version
        id: extract_version
        uses: Saionaro/extract-package-version@v1.0.6

      - name: Rename Files
        id: rename_files
        run: |
          cp "releases/Lindo ${{ steps.extract_version.outputs.version }}.exe" "releases/Lindo latest.exe"

      - name: Create windows artifact
        uses: actions/upload-artifact@v2
        with:
          name: win
          path: |
            releases/Lindo latest.exe
            releases/Lindo Setup latest.exe
            releases/Lindo-latest-win.zip
            releases/Lindo-latest-ia32-win.zip

  release-linux:
    name: "Release linux"
    runs-on: ubuntu-20.04

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      #utiliser node v14 ? https://github.com/actions/setup-node
      - run: npm i
      - run: npm run build:prod
      - run: npm run release:linux

      - name: Extract version
        id: extract_version
        uses: Saionaro/extract-package-version@v1.0.6

      - name: Create linux artifact
        uses: actions/upload-artifact@v2
        with:
          name: lin
          path: |
            releases/lindo-latest.tar.gz
            releases/lindo_latest_amd64.deb

  release-macOS:
    name: "Release macOS"
    runs-on: macos-10.15

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      #utiliser node v14 ? https://github.com/actions/setup-node
      - run: npm i
      - run: npm run build:prod
      - name: release mac
        run: |
          npm run release:mac
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version
        id: extract_version
        uses: Saionaro/extract-package-version@v1.0.6

      - name: Create macOS artifact
        uses: actions/upload-artifact@v2
        with:
          name: mac
          path: |
            releases/Lindo-latest.dmg
            releases/Lindo-latest-mac.zip

  upload-release:
    name: "Upload Release"
    runs-on: ubuntu-20.04
    needs: [release-win, release-linux, release-macOS]

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      #update pachage.json?
      - run: npm i
      #on dowload tous les artifacts
      - uses: actions/download-artifact@v2

      - name: Extract version
        id: extract_version
        uses: Saionaro/extract-package-version@v1.0.6

      - name: Rename Files
        id: rename_files
        run: |
          cp "win/Lindo ${{ steps.extract_version.outputs.version }}.exe" "win/Lindo latest.exe"

      - name: Automatic Release
        uses: "marvinpinto/action-automatic-releases@latest"
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          prerelease: true
          automatic_release_tag: "latest"
          title: "latest"
          files: |
            win/Lindo latest.exe
            win/Lindo ${{ steps.extract_version.outputs.version }}.exe
            win/Lindo Setup latest.exe
            win/Lindo-latest-win.zip
            win/Lindo-latest-ia32-win.zip
            lin/lindo-latest.tar.gz
            lin/lindo_latest_amd64.deb
            mac/Lindo-latest.dmg
            mac/Lindo-latest-mac.zip
